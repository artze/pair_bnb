<div>
	<%= f.label :booking_start %>
	<%= f.date_select :booking_start, {order: [:day, :month, :year], prompt: { day: 'Select day', month: 'Select month', year: 'Select year'}}, {required: true} %>
</div>

<div>
	<%= f.label :booking_end %>
	<%= f.date_select :booking_end, {order: [:day, :month, :year], prompt: { day: 'Select day', month: 'Select month', year: 'Select year'}}, {required: true} %>
</div>