<% @reservations.each do |reservation| %>
	<tr>
		<td> <a href="<%= user_reservation_path(current_user, reservation) %>"><%= reservation.listing.title %> </a></td>
		<td><%= reservation.booking_start %></td>
		<td><%= reservation.booking_end %></td>
	</tr>
<% end %>